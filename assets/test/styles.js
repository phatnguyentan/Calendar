
import {
  StyleSheet
} from "react-native";
import COLORS from "./colors";

const STYLES = StyleSheet.create({
  list: {
    backgroundColor: COLORS.LIST
  },
  listItem: {
    backgroundColor: COLORS.LIST_ITEM,
  }
});

export default STYLES;